To effectively build this pipeline, instruct your AI coding agent step-by-step through the following architectural phases.Phase 1: Environment & Dataset InitializationInstruct the agent to set up the dataloaders for two distinct phases: Base Pretraining and Self-Supervised Inpainting.Base Generation Setup: Point the dataloader to the G-Objaverse dataset to fine-tune the GSVAE so it accurately learns to compress the splat properties into the latent grid [cite: 92].Self-Supervised Masking Setup: For the inpainting task, instruct the agent to utilize DL3DV-10K, a massive real-world multi-view dataset containing 51.2 million frames across 10,510 videos [cite: 264, 336]. The agent must implement a function to randomly apply 3D volumetric masks during the training loop, forcing the model to reconstruct the dropped-out geometry based solely on unmasked context [cite: 241, 313].Benchmarking: Set up evaluation scripts using SPIn-NeRF (for front-facing scenes) and Inpaint360GS (for full 360-degree scenes) to calculate quantitative metrics on object removal [cite: 168, 296].Phase 2: Implementing the GSVAE MappingInstruct the agent to modify a pretrained SDXL Variational Autoencoder (VAE) to accept a 12-channel input tensor instead of a standard 3-channel RGB image.The 12-Channel Grid: The agent must explicitly construct the input tensor $G_i \in \mathbb{R}^{12 \times H \times W}$ utilizing the exact property order mapped in DiffSplat:RGB Color $c \in \mathbb{R}^3$ (3 channels)Predicted Depth $d \in \mathbb{R}$ (1 channel, used to derive Location $x$)Scale $s \in \mathbb{R}^3$ (3 channels)Rotation Quaternion $r \in \mathbb{R}^4$ (4 channels)Opacity $o \in \mathbb{R}$ (1 channel) [cite: 92]Duplicating Weights: To adapt the SDXL VAE, instruct the agent to duplicate the pretrained input and output convolution weights 4 times to accept the 12 channels without destroying the pre-learned natural image frequencies [cite: 92].Phase 3: Implementing SAM 3 for Semantic MaskingTo automate object and shadow removal, integrate Meta's Segment Anything Model 3 (SAM 3).Promptable Concept Segmentation (PCS): Instruct the agent to utilize SAM 3's PCS API. The agent must pass a short noun phrase (e.g., "the wooden chair") to generate masks [cite: 142]. Tell the agent to utilize SAM 3's "presence head" logic to verify the object actually exists before attempting localization to reduce false positives [cite: 209].Shadow Association (GauComp Logic): Using SAM 3, prompt for both the object and its cast shadow independently. The agent must implement a function that calculates the spatial center of the shadow mask and the spatial center of the object mask, saving an "offset vector" that links the two [cite: 28, 68]. When the object is queried for deletion, the agent's script must automatically query this offset vector to delete the shadow latents simultaneously [cite: 104, 137].Phase 4: Constructing the Inpainting Diffusion LoopThis is the core of the implementation. The agent must build a custom PyTorch training loop that modifies the denoising trajectory inside the latent space.Latent Deletion: Based on the SAM 3 multi-view masks, zero out the corresponding indices in the 4-channel GSVAE latent grid.Attention Injection (3D-LATTE Logic): Instruct the agent to implement an exact DDPM inversion on the unedited 3DGS scene [cite: 96, 190]. During the forward denoising pass (inpainting), the agent must write hooks into the U-Net to inject the original 3D self-attention maps and 3D cross-attention maps into the current pass [cite: 10, 190]. This mathematically freezes the unedited background structures while hallucinating the missing void.Phase 5: Formulating the Min-SNR Dual LossThe agent must implement a hybrid loss function that balances 2D diffusion priors with strict 3D rendering constraints.The Loss Equation: Instruct the agent to code the total loss: $\mathcal{L}_{total} = \lambda_{diff} \cdot \mathcal{L}_{diff} + \lambda_{render} \cdot \omega_r(t) \cdot \mathcal{L}_{render}$ [cite: 97].SNR Weighting (Crucial): To prevent the rendering loss from producing catastrophic gradients at early, noisy timesteps, the agent must implement the Min-SNR-$\gamma$ weighting strategy [cite: 105]. The code should calculate the Signal-to-Noise Ratio at timestep $t$ ($SNR(t) = \alpha_t^2 / \sigma_t^2$) and clamp the rendering loss weight $\omega_r(t)$ dynamically [cite: 105, 249].Phase 6: Depth-Guided Geometry CompletionFinally, to ensure the inpainted latents form physically sound geometry, implement depth-guided reprojection.SplatFill Error Heatmap: Instruct the agent to use the 3DGS differentiable rasterizer to render a depth map of the newly inpainted void. Compare this against a monocular depth estimator's output of the same view to generate a per-pixel absolute difference heatmap [cite: 118].Optimization: Apply a weighted depth optimization loss based on this heatmap to iteratively adjust the newly generated Gaussians along their camera rays until they align perfectly with the surrounding unmasked geometry [cite: 118].